# Minimal runtime dependencies for Streamlit Cloud

streamlit==1.28.1
streamlit-elements==0.1.0
# pandas removed to avoid heavy C build on Streamlit Cloud; optional at dev-time
# If you need pandas features in production, add it via a custom image or ensure
# the runtime supports prebuilt wheels for your Python version.
plotly==5.18.0
requests==2.31.0
python-dotenv==1.0.0

