# Minimal runtime dependencies for Streamlit Cloud
# Keep dev tools in requirements-dev.txt
streamlit==1.28.1
streamlit-elements==0.1.0
pandas==2.1.3
plotly==5.18.0
requests==2.31.0
python-dotenv==1.0.0

# Optional runtime extras (uncomment if you use them in production)
# sqlalchemy==2.0.23
